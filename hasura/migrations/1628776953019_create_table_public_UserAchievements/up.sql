CREATE TABLE "public"."UserAchievements" ("id" uuid NOT NULL, "additionalInfo" text, "created_at" timestamptz NOT NULL, "label" text NOT NULL, "points" integer NOT NULL, "step" integer NOT NULL, "userEmail" text NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("userEmail") REFERENCES "public"."User"("email") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"), UNIQUE ("userEmail", "label", "additionalInfo", "step"));
